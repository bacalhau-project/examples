NameProvider: hostname
Compute:
  Enabled: true
  Orchestrators:
    - orchestrator:4222
  Env:
    AllowList:
      - "AWS_*"
  AllowListedLocalPaths:
    - "/app/**:rw"

UpdateConfig:
  Interval: 0 